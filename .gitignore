node_modules
.next
out
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Convex codegen is committed so Vercel can build from the repo.
# Regenerated locally by `convex dev` / `convex codegen`.
convex/_generated/ai/
.vercel
